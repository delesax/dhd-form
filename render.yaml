
services:
  - type: web
    name: universal-downloader
    env: python
    buildCommand: pip install -r requirements.txt && playwright install --with-deps chromium
    startCommand: streamlit run universal_downloader.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
